# Automation scenarios

This file intentionally mirrors the Hedy-native workflow requirements in `../SCENARIOS.md`.

The previous exported workflows were retired because they depended directly on the superseded database architecture. Reintroduction requires Hedy-native identifiers, signed provider ingress, replay-safe event handling, explicit tenant/identity boundaries, and governed activation.
